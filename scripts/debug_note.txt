import requests
import uuid

BASE_URL = "http://localhost:8001/api"
# Need a token. I'll get the specialist ID and try to bypass if possible, 
# but usually I can't easily. 
# Better: check the database state after the user's attempt.

# Wait, I can see the database state. 
# The user said it shows 600 in the list but 630 in modal.
# This means the GET /api/presupuestos is returning 600.
